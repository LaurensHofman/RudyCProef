<Window x:Class="Rudycommerce.NewDesktopUser"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Rudycommerce"
        mc:Ignorable="d"
        Title="NewDesktopUser" Height="300" Width="300">
    <Grid>
        <Label FontSize="16" Content="{DynamicResource ResourceKey=nduTitle}" HorizontalContentAlignment="Center" Margin="10,10,9.4,0" VerticalAlignment="Top" Height="47"/>
        <Button x:Name="btnSave" FontSize="16" Content="{DynamicResource ResourceKey=nduSave}" Margin="0,41,10,0" VerticalAlignment="Top" Height="30" HorizontalAlignment="Right" Width="75" Click="btnSave_Click"/>

        <Label Content="{DynamicResource ResourceKey=nduFirstName}" FontSize="16" HorizontalAlignment="Left" Margin="10,47,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="txtFirstName" FontSize="16" VerticalContentAlignment="Center" HorizontalAlignment="Left" Height="30" Margin="10,84,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="280"/>

        <Label Content="{DynamicResource ResourceKey=nduLastName}" FontSize="16" HorizontalAlignment="Left" Margin="10,127,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="txtLastName" FontSize="16" VerticalContentAlignment="Center" HorizontalAlignment="Left" Height="30" Margin="10,164,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="280"/>

        <Label Content="{DynamicResource ResourceKey=nduUsername}" FontSize="16" HorizontalAlignment="Left" Margin="10,215,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="txtUsername" FontSize="16" VerticalContentAlignment="Center" HorizontalAlignment="Left" Height="30" Margin="10,252,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="280"/>

        <Label Content="{DynamicResource ResourceKey=nduEmail}" FontSize="16" HorizontalAlignment="Left" Margin="10,295,0,0" VerticalAlignment="Top"/>
        <TextBox x:Name="txtEmail" FontSize="16" VerticalContentAlignment="Center" HorizontalAlignment="Left" Height="30" Margin="10,332,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="280"/>

        <Label Content="{DynamicResource ResourceKey=nduPassword}" FontSize="16" HorizontalAlignment="Left" Margin="10,376,0,0" VerticalAlignment="Top"/>
        <PasswordBox x:Name="pwdPassword" VerticalContentAlignment="Center" Height="30" HorizontalAlignment="Left" Margin="10,413,0,0" VerticalAlignment="Top" Width="280" />

        <Label Content="{DynamicResource ResourceKey=nduPrefLang}" FontSize="16" HorizontalAlignment="Left" Margin="10,448,0,0" VerticalAlignment="Top"/>

        <WrapPanel Margin="0,480,0,0" Height="40" VerticalAlignment="Top">
            <WrapPanel.Resources>
                <Style x:Key="Flag" TargetType="RadioButton">
                    <Style.Triggers>
                        <Trigger Property="IsChecked" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter Property="BorderBrush" Value="Black"/>
                            <Setter Property="BorderThickness" Value="3"/>
                        </Trigger>
                    </Style.Triggers>

                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="RadioButton">
                                <Border BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Background="Transparent"
                                    CornerRadius="20">
                                    <Image Source="{Binding Path=Content, RelativeSource={RelativeSource TemplatedParent}}"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </WrapPanel.Resources>
            <RadioButton x:Name="rbPreferNL" GroupName="Lang"  Content="../Resources/Icons/32_NL.png" Style="{StaticResource Flag}" Margin="10,0,10,0" Width="40" Height="40" Checked="RadioButton_Checked"/>
            <RadioButton x:Name="rbPreferEN" GroupName="Lang" Content="../Resources/Icons/32_EN.png" Style="{StaticResource Flag}" Margin="10,0,10,0" Width="40" Height="40" Checked="RadioButton_Checked"/>
        </WrapPanel>
    </Grid>
</Window>
