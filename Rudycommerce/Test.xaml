<UserControl x:Class="Rudycommerce.Test"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rudycommerce"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid x:Name="grdOverview">
       
        <DataGrid Name="grdDVDOverview" Margin="0,30,0,0" AutoGenerateColumns="False" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Name}" Header="Name" />
                <DataGridTextColumn Binding="{Binding Path=Description}" Header="Description" />
                <DataGridTextColumn Binding="{Binding Path=ProductID}" Header="ID Prod" />
                <DataGridTextColumn Binding="{Binding Path=LanguageID}" Header="ID Lang" />
                <DataGridTextColumn Binding="{Binding Path=ProductPrice}" Header="Prod Price" />
                <DataGridTextColumn Binding="{Binding Path=LanguageName}" Header="LangName" />

            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
