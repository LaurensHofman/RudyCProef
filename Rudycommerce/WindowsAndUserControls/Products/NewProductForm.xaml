<UserControl x:Class="Rudycommerce.NewProductForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:Rudycommerce"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="1200"
             Background="Beige">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/LanguageResources/Dutch.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid x:Name="grdNewProductForm" Margin="0,10,0,0">
        <Controls:MetroAnimatedSingleRowTabControl x:Name="AnimatedTabControl" HorizontalAlignment="Stretch" SelectionChanged="AnimatedTabControl_SelectionChanged">

            <Controls:MetroAnimatedSingleRowTabControl.Resources>

                <Style TargetType="{x:Type TabPanel}">
                
                    <Setter Property="HorizontalAlignment" Value="Center"/>

                </Style>

            </Controls:MetroAnimatedSingleRowTabControl.Resources>

            <TabItem x:Name="tabItemGeneral" Header="{DynamicResource ResourceKey=General}" Controls:ControlsHelper.HeaderFontSize="22" Background="Beige"
                     VerticalAlignment="Top" HorizontalAlignment="Left" Foreground="Black" Padding="25,0,25,0" BorderThickness="1,0,1,0">
                <!--<TabItem.Style>
                    <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="BorderThickness" Value="2,2,2,2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </TabItem.Style>-->
                <Grid x:Name="grdGeneral" VerticalAlignment="Top">
                    <StackPanel>
                        <WrapPanel HorizontalAlignment="Center">
                            <StackPanel>
                                <Label Foreground="Black" FontSize="18" Height="30" HorizontalAlignment="Right" Margin="0,20,0,0" Padding="0,-5,0,-5">
                                    <WrapPanel>
                                        <TextBlock Text="{DynamicResource ResourceKey=SelectCategory}"/>
                                        <TextBlock Text=" : "/>                                        
                                    </WrapPanel>
                                </Label>
                                <Label Foreground="Black" FontSize="18" Height="30" HorizontalAlignment="Right" Margin="0,20,0,0" Padding="0,-5,0,-5">
                                    <WrapPanel>
                                        <TextBlock Text="{DynamicResource ResourceKey=InitialStock}"/>
                                        <TextBlock Text=" : "/>
                                    </WrapPanel>
                                </Label>
                                <Label Foreground="Black" FontSize="18" Height="30" HorizontalAlignment="Right" Margin="0,20,0,0" Padding="0,-5,0,-5">
                                    <WrapPanel>
                                        <TextBlock Text="{DynamicResource ResourceKey=UnitPrice}"/>
                                        <TextBlock Text=" : "/>
                                    </WrapPanel>
                                </Label>
                                <Label Foreground="Black" FontSize="18" Height="30" HorizontalAlignment="Right" Margin="0,20,0,0" Padding="0,-5,0,-5">
                                    <WrapPanel>
                                        <TextBlock Text="{DynamicResource ResourceKey=IsProductActive}"/>
                                        <TextBlock Text=" : "/>
                                    </WrapPanel>
                                </Label>
                            </StackPanel>
                            <StackPanel>
                                <ComboBox x:Name="cmbxCategories" Margin="0,20,0,0" Width="200" Height="30" Padding="0,5,0,5" 
                                            ItemsSource="{Binding Path=CategoryItemList}"
                                            DisplayMemberPath="Name"
                                            SelectedValuePath="CategoryID"
                                            SelectedValue="{Binding Path=ProductModel.CategoryID}" SelectionChanged="cmbxCategories_SelectionChanged"/>
                                <TextBox Text="{Binding Path=ProductModel.InitialStock}" VerticalContentAlignment="Center" HorizontalAlignment="Left" Margin="0,20,0,0"  Height="30"  Width="50"/>
                                <TextBox Text="{Binding Path=ProductModel.UnitPrice}" VerticalContentAlignment="Center" HorizontalAlignment="Left" Margin="0,20,0,0" Height="30"  Width="50"/>
                                <CheckBox IsChecked="{Binding Path=ProductModel.IsActive}" VerticalContentAlignment="Center" HorizontalAlignment="Left" Margin="0,20,0,0" Height="30" Width="50"/>
                            </StackPanel>
                        </WrapPanel>
                        <WrapPanel Margin="0,100,0,0" HorizontalAlignment="Center">
                            <Image Source="/Resources/Icons/32_NL.png" Width="150" Height="150" Margin="20,0,20,0"></Image>
                            <Image Source="/Resources/Icons/32_NL.png" Width="150" Height="150" Margin="20,0,20,0"></Image>
                            <Image Source="/Resources/Icons/32_NL.png" Width="150" Height="150" Margin="20,0,20,0"></Image>
                            <Image Source="/Resources/Icons/32_NL.png" Width="150" Height="150" Margin="20,0,20,0"></Image>
                        </WrapPanel>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem x:Name="tabItemMultilingualProperties" Visibility="Collapsed" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="25,0,25,0" Background="Beige" BorderThickness="1,0,1,0"
                     Header="{DynamicResource ResourceKey=MultilingualSpecificProperties}" Controls:ControlsHelper.HeaderFontSize="22">
                <Grid x:Name="grdMultilingualProperties" VerticalAlignment="Top"  Margin="0,20,0,0">
                    <Controls:MetroAnimatedSingleRowTabControl x:Name="TabControlLanguages" HorizontalAlignment="Center">
                        <Controls:MetroAnimatedSingleRowTabControl.Resources>
                            <Style TargetType="{x:Type TabPanel}">
                                <Setter Property="HorizontalAlignment" Value="Center"/>
                            </Style>
                        </Controls:MetroAnimatedSingleRowTabControl.Resources>
                        <!-- content will be created in c# code -->
                    </Controls:MetroAnimatedSingleRowTabControl>
                </Grid>
            </TabItem>
            <TabItem x:Name="tabItemNonMultilingualProperties" Visibility="Collapsed" VerticalAlignment="Top" HorizontalAlignment="Center" Padding="25,0,25,0" Background="Beige" BorderThickness="1,0,1,0"
                     Header="{DynamicResource ResourceKey=NonMultilingualSpecificProperties}" Controls:ControlsHelper.HeaderFontSize="22">
                <Grid x:Name="grdNonMultilingualProperties" VerticalAlignment="Top" Margin="0,20,0,0" HorizontalAlignment="Center">
                    <WrapPanel HorizontalAlignment="Center">
                        <StackPanel x:Name="NonMLStackLeftLabels"></StackPanel>
                        <StackPanel x:Name="NonMLStackRightInput"></StackPanel>
                    </WrapPanel>                    
                </Grid>
            </TabItem>
        </Controls:MetroAnimatedSingleRowTabControl>
        <WrapPanel VerticalAlignment="Bottom" HorizontalAlignment="Right">
            <Button x:Name="btnCancel" Content="{DynamicResource ResourceKey=Cancel}" Width="120" FontSize="16" Click="btnCancel_Click" Margin="20,0,20,0"/>
            <Button x:Name="btnSubmit" Content="{DynamicResource ResourceKey=Save}" Width="120" FontSize="16" Click="btnSubmit_Click" Margin="20,0,20,0" />
        </WrapPanel>
    </Grid>
</UserControl>
