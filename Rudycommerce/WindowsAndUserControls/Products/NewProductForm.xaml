<UserControl x:Class="Rudycommerce.NewProductForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:Controls="http://metro.mahapps.com/winfx/xaml/controls"
             xmlns:local="clr-namespace:Rudycommerce"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="1200"
             Background="Beige">

    <UserControl.Resources>
        <ResourceDictionary>

            <Style TargetType="Controls:MetroAnimatedSingleRowTabControl" BasedOn="{StaticResource {x:Type Controls:MetroAnimatedSingleRowTabControl}}">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="HorizontalAlignment" Value="Stretch"/>
            </Style>

            <Style TargetType="TabItem" BasedOn="{StaticResource {x:Type TabItem}}">
                <Setter Property="Foreground" Value="Black"/>
                <Setter Property="Background" Value="Beige"/>
                <Setter Property="Controls:ControlsHelper.HeaderFontWeight" Value="SemiBold"/>
                <Setter Property="Padding" Value="25,0,25,0"/>
                <Setter Property="Height" Value="39"/>
                <Setter Property="BorderBrush" Value="Black"/>
                <Setter Property="BorderThickness" Value="1,0,1,0"/>
            </Style>

        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid x:Name="grdNewProductForm" Style="{StaticResource ResourceKey=UserControlGrid}">

            <Controls:MetroAnimatedSingleRowTabControl x:Name="AnimatedTabControl" SelectionChanged="AnimatedTabControl_SelectionChanged">

                <Controls:MetroAnimatedSingleRowTabControl.Resources>

                    <Style TargetType="{x:Type TabPanel}">

                        <Setter Property="HorizontalAlignment" Value="Center"/>

                    </Style>

                </Controls:MetroAnimatedSingleRowTabControl.Resources>

                <TabItem x:Name="tabItemGeneral" Header="{DynamicResource ResourceKey=General}" Controls:ControlsHelper.HeaderFontSize="22">
                    <Grid x:Name="grdGeneral" Style="{StaticResource ResourceKey=GridBelowTabItem}">
                        <StackPanel>
                            <WrapPanel HorizontalAlignment="Center">
                                <StackPanel>
                                    <Label Style="{StaticResource ResourceKey=FormLabel}">
                                        <WrapPanel>
                                            <TextBlock Text="{DynamicResource ResourceKey=SelectCategory}"/>
                                            <TextBlock Text=" : "/>
                                        </WrapPanel>
                                    </Label>
                                    <Label Style="{StaticResource ResourceKey=FormLabel}">
                                        <WrapPanel>
                                            <TextBlock Text="{DynamicResource ResourceKey=InitialStock}"/>
                                            <TextBlock Text=" : "/>
                                        </WrapPanel>
                                    </Label>
                                    <Label Style="{StaticResource ResourceKey=FormLabel}">
                                        <WrapPanel>
                                            <TextBlock Text="{DynamicResource ResourceKey=UnitPrice}"/>
                                            <TextBlock Text=" : "/>
                                        </WrapPanel>
                                    </Label>
                                    <Label Style="{StaticResource ResourceKey=FormLabel}">
                                        <WrapPanel>
                                            <TextBlock Text="{DynamicResource ResourceKey=IsProductActive}"/>
                                            <TextBlock Text=" : "/>
                                        </WrapPanel>
                                    </Label>
                                </StackPanel>
                                <StackPanel>
                                    <ComboBox x:Name="cmbxCategories" Style="{StaticResource ResourceKey=FormInputComboBox}" Width="400" Padding="0,5,0,5" 
                                            ItemsSource="{Binding Path=CategoryItemList}"
                                            DisplayMemberPath="Name"
                                            SelectedValuePath="CategoryID"
                                            SelectedValue="{Binding Path=ProductModel.CategoryID}" SelectionChanged="cmbxCategories_SelectionChanged"/>
                                    <TextBox Text="{Binding Path=ProductModel.InitialStock}" Style="{StaticResource ResourceKey=FormInputTextBox}" Width="50"/>
                                    <TextBox Text="{Binding Path=ProductModel.UnitPrice}" Style="{StaticResource ResourceKey=FormInputTextBox}" Width="50"/>
                                    <CheckBox IsChecked="{Binding Path=ProductModel.IsActive}" Style="{StaticResource ResourceKey=FormInputCheckBox}" Width="50"/>
                                </StackPanel>
                                <TextBlock>Here</TextBlock>
                            </WrapPanel>
                            <WrapPanel Margin="0,100,0,0" HorizontalAlignment="Center">
                                <Image Source="/Resources/Icons/32_NL.png" Width="150" Height="150" Margin="20,0,20,0"></Image>
                                <Image Source="/Resources/Icons/32_NL.png" Width="150" Height="150" Margin="20,0,20,0"></Image>
                                <Image Source="/Resources/Icons/32_NL.png" Width="150" Height="150" Margin="20,0,20,0"></Image>
                                <Image Source="/Resources/Icons/32_NL.png" Width="150" Height="150" Margin="20,0,20,0"></Image>
                            </WrapPanel>
                        </StackPanel>
                    </Grid>
                </TabItem>
                <TabItem x:Name="tabItemMultilingualProperties" Visibility="Collapsed" VerticalAlignment="Top" HorizontalAlignment="Left" Padding="25,0,25,0" Background="Beige" BorderThickness="1,0,1,0"
                     Header="{DynamicResource ResourceKey=MultilingualSpecificProperties}" Controls:ControlsHelper.HeaderFontSize="22">
                    <Grid x:Name="grdMultilingualProperties" VerticalAlignment="Top"  Margin="0,20,0,0">
                        <Controls:MetroAnimatedSingleRowTabControl x:Name="TabControlLanguages" HorizontalAlignment="Center">
                            <Controls:MetroAnimatedSingleRowTabControl.Resources>
                                <Style TargetType="{x:Type TabPanel}">
                                    <Setter Property="HorizontalAlignment" Value="Center"/>
                                </Style>
                            </Controls:MetroAnimatedSingleRowTabControl.Resources>
                            <!-- content will be created in c# code -->
                        </Controls:MetroAnimatedSingleRowTabControl>
                    </Grid>
                </TabItem>
                <TabItem x:Name="tabItemNonMultilingualProperties" Visibility="Collapsed" VerticalAlignment="Top" HorizontalAlignment="Center" Padding="25,0,25,0" Background="Beige" BorderThickness="1,0,1,0"
                     Header="{DynamicResource ResourceKey=NonMultilingualSpecificProperties}" Controls:ControlsHelper.HeaderFontSize="22">
                    <Grid x:Name="grdNonMultilingualProperties" VerticalAlignment="Top" Margin="0,20,0,0" HorizontalAlignment="Center">
                        <WrapPanel HorizontalAlignment="Center">
                            <StackPanel x:Name="NonMLStackLeftLabels"></StackPanel>
                            <StackPanel x:Name="NonMLStackRightInput"></StackPanel>
                        </WrapPanel>
                    </Grid>
                </TabItem>
            </Controls:MetroAnimatedSingleRowTabControl>
            <WrapPanel HorizontalAlignment="Right" VerticalAlignment="Bottom">
                <Button x:Name="btnCancel" Content="{DynamicResource ResourceKey=Cancel}" Width="120" FontSize="16" Click="btnCancel_Click" Margin="20"/>
                <Button x:Name="btnSubmit" Content="{DynamicResource ResourceKey=Save}" Width="120" FontSize="16" Click="btnSubmit_Click" Margin="20" />
            </WrapPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
